<body>
    <nav class="navbar">
        <h1>JobFinder</h1>
    <div class="search-container">
            <form action="/empresa/candidatos/buscar" method="GET">
                <span class="search-icon"></span>
                <input type="text" name="q" class="search-input" placeholder="Buscar Candidatos 🔍">
                <button type="submit" class="search-button">Buscar</button>
            </form>
    </div>
        <ul class="navbar-menu">
            <li><a href="/login">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                class="bi bi-box-arrow-right" viewBox="0 0 16 16">
                <path fill-rule="evenodd"
                    d="M10 12.5a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-9a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v2a.5.5 0 0 0 1 0v-2A1.5 1.5 0 0 0 9.5 2h-8A1.5 1.5 0 0 0 0 3.5v9A1.5 1.5 0 0 0 1.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-2a.5.5 0 0 0-1 0z" />
                <path fill-rule="evenodd"
                    d="M15.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 0 0-.708.708L14.293 7.5H5.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708z" />
            </svg>
            </a></li>
        </ul>
    </nav>

    <!-- Barra de busca -->


    <main class="main-content">
    <!-- Abas de categorias -->
        <div class="category-tabs">
            <button class="tab active">Todos</button>
            <button class="tab">Área de TI</button>
            <button class="tab">Estoque</button>
            <button class="tab">Caixa</button>
            <button class="tab">Vendas</button>
        </div>

        <!-- Grid de candidatos -->
        <div class="candidates-grid">
            {{#each candidatos}}
            <div class="candidate-card">
                {{#if imagem}}
                <img src="{{imagem}}" alt="{{nome}}" class="candidate-image" />
                {{/if}}
                <h3 class="candidate-name">{{nome}}</h3>
                <p class="candidate-qualification">{{qualificacao}}</p>
                <button class="view-profile-button">Ver Perfil</button>
            </div>
            {{/each}}
        </div>
    </main>
    <!-- Navegação inferior -->
    <nav class="bottom-nav">
        <a href="/empresa/dashboard" class="nav-item active">Início</a>
        <a href="/empresa/{{empresaId}}/vagas" class="nav-item">Vagas</a>
        <a href="/empresa/{{empresaId}}/vagas/criar" class="nav-item">Add Vagas</a>
        <a href="/empresa/{{empresaId}}/perfil" class="nav-item">Perfil</a>
    </nav>
</body>

</html>